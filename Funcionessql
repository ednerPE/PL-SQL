set serveroutput on 
DECLARE
X VARCHAR2(50);
FECHA DATE;
BEGIN
X:='BUENOS DIAS';
--UTILIZANDO LA FUNCION DE SUBTRAER 
    DBMS_OUTPUT.PUT_LINE(SUBSTR(X,5,6));
--UTILIZANDO LA FUNCION FECHA PARA EXTRER LA FECHA DEL SISTEMA
    FECHA:=SYSDATE;
    DBMS_OUTPUT.PUT_LINE(FECHA);
END;
