SET SERVEROUTPUT ON
--Averiguar el dia que naciste utilizando PL/SQL.
DECLARE
    FEC_NAC DATE;
    DIA_SEMANA VARCHAR2(100);
BEGIN
    FEC_NAC:=TO_DATE('02/01/1995');
    DIA_SEMANA:=TO_CHAR(FEC_NAC,'DAY');
    DBMS_OUTPUT.PUT_LINE(DIA_SEMANA);
END;